# This is a Databricks asset bundle definition for New_Project_01.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: New_Project_01
  uuid: 3f23ff71-9037-4f2e-b1ba-64d9510dba74

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-2faacb94-1829.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-2faacb94-1829.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/sibgath@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/sibgath@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: sibgath@gmail.com
        level: CAN_MANAGE
