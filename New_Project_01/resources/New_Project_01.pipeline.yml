# The main pipeline for New_Project_01
resources:
  pipelines:
    New_Project_01_pipeline:
      name: New_Project_01_pipeline
      catalog: workspace
      schema: New_Project_01_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
