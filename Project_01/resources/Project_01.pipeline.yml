# The main pipeline for Project_01
resources:
  pipelines:
    Project_01_pipeline:
      name: Project_01_pipeline
      catalog: workspace
      schema: Project_01_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
